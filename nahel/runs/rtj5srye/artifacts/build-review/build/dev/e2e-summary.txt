CHANGE SUMMARY — item cycx4815 (e2e), run rtj5srye, agent:opus-dev

Added tests/e2e/game.test.ts (one file, 3 tests, no product changes). Commit 0c4f62a on epic/murder-mystery-game.
1. Full LLM-path game: real runGame + real ChatClient over the in-process fake vendor, seed 1, 3 seats. Asserts the opening is the vendor's fiction (not the canned scenario), turns narrated in the vendor's words, one Q&A answered in character with the typed question present in the prompt, the human seat's accusation winning the game, and the reveal naming the engine's dealt triple exactly once.
2. Degraded call without the breaker: one narration call is refused with HTTP 400 (not retried, unlike 429/5xx), so exactly one GuardedFailure lands under LLM_FAILURE_LIMIT=2. Vendor-marked lines exist before AND after the single "(the game master is not answering)" notice, the line right after it is a plain engine sentence, and "LLM disabled for this session" never appears.
3. Ledger checked against the serving side: the vendor records every usage object it serves with per-call-varying figures; the printed "N calls, 1 failed attempt, T tokens (P prompt + C completion)" line and the per-model line are compared to those sums, with vendor.requests.length == served.length + 1.
4. Offline via the real binary: `bun run src/cli.ts --no-llm --seed 5 --players 3` spawned from an mkdtemp cwd (never the repo — the .env.local live-call hazard) with POE_API_KEY emptied, fed a scripted answer file, exits 0, reaches the reveal, prints "No LLM calls were made this session.", and never mentions a game master.
5. Determinism across processes: the same offline game run twice in separate processes, stdout compared byte for byte with nothing stripped.
Deliberately not duplicated: menu resolution, bad input, notebook, refutation contents, --help and argument errors (tests/ui/*).

Bar: whole-repo `bun test` 343 pass / 0 fail across 19 files (340 baseline + 3). `bunx tsc --noEmit` exit 0.
Defects: none. The loop, game master, client and CLI already joined up correctly — this item is all test, no fix.
Assertions mutation-checked: serving usage:null and moving the failing narration call out of range each fail the test.
